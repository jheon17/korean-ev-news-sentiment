# 프로젝트: 전기차 뉴스 감성 분류

## 프로젝트 성격

공개 뉴스 검색 결과를 이용하여 전기차 시장과 등록대수에 미칠 영향을
긍정, 중립, 부정으로 분류하는 학습 프로젝트다.

과거 회사 데이터와 결과를 복원하는 프로젝트가 아니다.
README에는 공개 데이터 기반 방법론 재구현이라고 명시한다.

## 사용자의 수준과 협업 방식

사용자는 Python과 자연어 처리 초급자다.
모든 설명, 코드 주석, 문서와 Git 커밋 메시지는 한국어로 작성한다.

전체 코드를 한 번에 작성하지 않는다.
한 단계가 끝날 때마다 사용자의 실행 결과를 확인한 후 다음 단계로 이동한다.

각 단계에서 다음을 제공한다.

1. 목적
2. 쉬운 개념 설명
3. 사용자가 먼저 생각할 질문
4. 최소 실행 코드
5. 결과 해석 방법
6. 오류·편향·데이터 누수 가능성
7. 완료 조건

## 개발 환경

Python 의존성과 가상환경은 `uv`로 관리한다.
의존성의 기준 파일은 `pyproject.toml`과 `uv.lock`이다.

- 기본 설치: `uv sync`
- 로컬 언어모델 포함 설치: `uv sync --extra local-models`
- Python 실행: `uv run python 경로/스크립트.py`

`pip install`과 `requirements.txt`를 별도의 설치 기준으로 병행하지 않는다.
문서와 코드의 실행 예시는 `uv run` 기준으로 작성한다.

## GitHub 공개 규칙

README에는 완료한 단계, 아직 진행하지 않은 단계, 평가 범위와 한계를 구분해 적는다.
라벨과 예측·평가 결과는 재현 근거로 보존하되, 공개 전 비밀값과 이용약관을 확인한다.

다음 항목은 Git에 올리지 않는다.

- `.env`와 실제 API 키
- `data/raw/`, `data/processed/`, `data/cache/`
- 가상환경, Python 캐시, 다운로드한 모델 가중치

GitHub에 올리기 전 `git status --ignored`와 비밀값 검사를 수행한다.

## 데이터 출처

네이버 뉴스 검색 API 설명서:

https://developers.naver.com/docs/serviceapi/search/news/news.md

애플리케이션 등록:

https://developers.naver.com/apps/#/register

네이버 API는 다운로드형 CSV가 아니다.
API를 호출하여 제목, 요약문, 원문 링크, 발행일을 수집하고 CSV로 저장한다.
기사 본문 전체를 임의로 크롤링하지 않는다.

## 보안 규칙

Client ID와 Client Secret을 코드에 직접 작성하지 않는다.
환경변수 또는 `.env`로 관리한다.
`.env`는 반드시 `.gitignore`에 추가한다.
실제 키를 출력하거나 README, 노트북, Git에 저장하지 않는다.
`.env.example`에는 변수 이름만 작성한다.

## 감성의 정의

이 프로젝트의 감성은 문장의 일반적인 기분이 아니다.
뉴스가 향후 전기차 시장 또는 등록대수에 미칠 것으로 예상되는 영향을 의미한다.

- 긍정: 수요·보급·등록 증가 가능성을 높이는 내용
- 중립: 사실 전달, 영향이 불명확하거나 긍정과 부정이 혼재된 내용
- 부정: 수요·보급·등록 감소 또는 위험 증가 가능성을 높이는 내용

예를 들어 사고 뉴스는 문장 자체가 부정적일 뿐 아니라
소비자 신뢰와 수요에 미치는 영향을 기준으로 판단한다.
판단 근거가 부족하면 억지로 긍정이나 부정으로 분류하지 않고 중립으로 처리한다.

## 진행 단계

### 1단계: 문제와 라벨 정의

분류 대상이 다음 중 무엇인지 사용자와 결정한다.

- 특정 기업에 미치는 영향
- 국내 전기차 시장 전체에 미치는 영향
- 향후 전기차 등록대수에 미치는 영향

라벨 가이드와 경계 사례를 먼저 문서로 작성한다.

### 2단계: API 연결

API 키를 안전하게 설정한다.
처음에는 검색 결과 10건만 호출한다.
응답 상태 코드와 데이터 구조를 확인한 후 다음 단계로 넘어간다.

### 3단계: 데이터 수집

검색어 후보를 사용자와 정한다.

예시:

- 전기차
- 전기차 보조금
- 전기차 판매
- 전기차 충전
- 배터리 화재
- 전기차 가격

검색어와 수집 날짜를 함께 기록한다.
API의 검색 범위, 페이지 제한, 정렬 방식과 과거 기사 접근 한계를 설명한다.
재실행 시 같은 데이터를 반복 요청하지 않도록 원본 응답 또는 CSV를 저장한다.

### 4단계: 전처리

다음을 처리한다.

- 제목과 요약문의 HTML 태그 제거
- 날짜 형식 통일
- 원문 링크 기준 중복 제거
- 제목이 거의 동일한 기사 확인
- 결측 제목·요약문 확인
- 검색어별 중복 수집 확인

원본 데이터와 전처리 데이터는 별도로 보존한다.

### 5단계: 사람이 만든 정답 데이터

모델을 평가하려면 사람이 직접 라벨링한 정답이 필요하다.
처음에는 약 100~200건을 목표로 하되 사용자와 규모를 결정한다.

라벨링 데이터는 개발용과 최종 테스트용으로 분리한다.
최종 테스트 라벨을 보면서 프롬프트를 수정하지 않는다.
라벨이 애매한 사례와 판단 근거를 별도 열에 기록한다.

### 6단계: 기준 모델

간단한 키워드 규칙 기반 분류기를 먼저 만든다.
복잡한 모델은 이 기준 모델과 비교한다.

### 7단계: 분류 방식 선택

다음 중 어떤 방식을 사용할지 사용자에게 먼저 묻는다.

1. 무료 키워드 및 수작업 기준 모델
2. 로컬 오픈소스 언어모델
3. 유료일 수 있는 LLM API

유료 API를 사용할 가능성이 있으면 예상 요청량과 비용 가능성을 설명하고
사용자의 확인 없이 실행하지 않는다.

LLM을 사용한다면 출력 형식을 고정된 JSON으로 설계한다.

필수 출력 예:

- label
- confidence
- reason
- evidence

허용하지 않은 라벨이나 잘못된 JSON을 검증하고 재시도·오류 로그·캐시를 구현한다.

### 8단계: 평가

Accuracy만 사용하지 않는다.

다음을 계산한다.

- Macro-F1
- 클래스별 Precision
- 클래스별 Recall
- 클래스별 F1
- 혼동행렬
- 분류하지 못한 오류 건수

특히 중립을 긍정 또는 부정으로 잘못 판단하는 사례를 분석한다.

### 9단계: 오류 분석

오류를 다음과 같이 분류한다.

- 제목만으로 맥락을 알기 어려움
- 요약문이 잘림
- 반어적 표현
- 기업과 시장 전체의 영향이 다름
- 단기 영향과 장기 영향이 다름
- 긍정·부정 요소가 동시에 존재
- 라벨 가이드 자체가 모호함

오류 사례를 근거로 프롬프트 또는 라벨 가이드를 수정한다.
최종 테스트 데이터는 수정 과정에 사용하지 않는다.

### 10단계: 월별 감성 지수

개별 뉴스 분류가 검증된 뒤에만 월별 감성 지수를 만든다.

예시:

(긍정 기사 수 - 부정 기사 수) / 전체 분류 기사 수

월별 기사 수가 너무 적으면 감성 지수를 과도하게 해석하지 않는다.
검색어 구성 변화와 중복 언론 기사로 인한 편향을 설명한다.

## 완료 기준

- API 키가 코드와 Git에 노출되지 않는다.
- 원본 데이터와 전처리 데이터를 분리한다.
- 감성 라벨의 의미가 문서화돼 있다.
- 사람이 만든 테스트 정답으로 평가한다.
- Accuracy 외에 Macro-F1과 클래스별 결과를 제공한다.
- 오류 사례와 데이터 한계를 정리한다.
- 공개 데이터 기반 재구현임을 명시한다.

## 첫 응답 규칙

아직 API 호출 코드 전체를 작성하지 않는다.

먼저 다음 내용만 설명한다.

1. 이 프로젝트에서 감성이 의미하는 것
2. 제목과 요약문만 사용할 때의 한계
3. 전체 과정을 초급자용으로 설명
4. 필요한 네이버 개발자 등록 절차
5. 사용자에게 확인할 질문 최대 3개
